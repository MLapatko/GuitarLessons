<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView android:layout_width="wrap_content"
    android:layout_height="@dimen/filters_height"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:cardCornerRadius="@dimen/cardView_filter_radius"
    xmlns:android="http://schemas.android.com/apk/res/android"
    app:cardBackgroundColor="@color/colorPrimary">

    <RadioGroup
        android:id="@+id/filters"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/radio_all"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginStart="@dimen/layout_margin"
            android:button="@android:color/transparent"
            android:checked="true"
            android:textColor="@drawable/filter_selector"
            android:text="@string/all" />
        <RadioButton
            android:id="@+id/radio_chords"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/layout_margin"
            android:button="@android:color/transparent"
            android:textColor="@drawable/filter_selector"
            android:text="@string/filter_chords" />

        <RadioButton
            android:id="@+id/radio_tabs"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/layout_margin"
            android:layout_marginStart="@dimen/layout_margin"
            android:button="@android:color/transparent"
            android:textColor="@drawable/filter_selector"
            android:text="@string/filter_tabs" />
    </RadioGroup>

</android.support.v7.widget.CardView>